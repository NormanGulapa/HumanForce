#Cypress Installation Guide with Cucumber:
•	npm install cypress
•	npm install -D @badeball/cypress-cucumber-preprocessor
•	npm install -D @bahmutov/cypress-esbuild-preprocessor
•	npm install -D esbuild
•	npm install
